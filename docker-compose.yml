services:
  postgres:
    image: postgres:16
    environment:
      POSTGRES_DB: brand_experience
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    ports:
      - "5432:5432"
    volumes:
      - brand_experience_pg:/var/lib/postgresql/data

volumes:
  brand_experience_pg:
